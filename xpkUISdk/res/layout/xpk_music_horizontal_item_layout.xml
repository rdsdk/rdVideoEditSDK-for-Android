<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:paddingLeft="10dp" >

    <LinearLayout
        android:layout_width="83dp"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/frag_music_menu_border_width"
        android:orientation="vertical" >

        <ImageView
            android:id="@+id/iv_music_item_thubmnail"
            android:layout_width="@dimen/music_item_width"
            android:layout_height="@dimen/music_item_height"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/tv_music_item_text"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@color/sub_menu_bgcolor"
            android:gravity="center"
            android:text="title"
            android:textColor="@color/white"
            android:textSize="@dimen/text_size_14" />
    </LinearLayout>

    <ImageView
        android:id="@+id/iv_music_item_selected"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/frag_music_menu_border_width"
        android:contentDescription="@null"
        android:src="@drawable/musci_item_selected_flag"
        android:visibility="visible" />

</RelativeLayout>